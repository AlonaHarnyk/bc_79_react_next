export interface Book {
  author: string;
  title: string;
  year: number;
  description: string;
  id: string;
}

export interface BookData{
  author: string;
  title: string;
  year: number;
  description: string;
}